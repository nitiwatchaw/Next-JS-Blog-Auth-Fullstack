/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com', 'images.unsplash.com' , 'pngimg.com', 'avatars.githubusercontent.com'],
  },
};

module.exports = nextConfig;
